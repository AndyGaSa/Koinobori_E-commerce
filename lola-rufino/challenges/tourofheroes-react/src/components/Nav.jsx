import React from 'react';
import '../styles.css';

export default function Nav() {
  const url = '';
  return (
    <nav>
      <a href={url}>Dashboard</a>
      <a href={url}>Heroes</a>
    </nav>
  );
}
