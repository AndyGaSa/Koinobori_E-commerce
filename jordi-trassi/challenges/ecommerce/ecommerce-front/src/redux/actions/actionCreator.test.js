
import axios from "axios";
import { payProducts } from "./actionCreators";

describe('Given payProductsc function', () => {
describe('When is invoked', () => {
    discribe('And resolves', () => {
        test('Then should call dispatch', async () => {
            const dispatch = jest.fn();
            axios.post.mock



        }
         )
    })
}))