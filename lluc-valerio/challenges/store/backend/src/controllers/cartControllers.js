const debug = require('debug')('storeApi:cartController');
const Cart = require('../models/cartModel');

function getAllCarts(req, res) {
  return 0;
}

function setCart(req, res) {
  return 0;
}

module.exports = {
  getAllCarts,
  setCart
};
