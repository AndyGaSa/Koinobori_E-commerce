/*
class changePage() {

    button() {
        this.offset= this.limit * this.page;
    }

    something() {
        let currentPage =  /
    }

    nextPage() {
        if (this.page === )
    }

    beforePage() {
        if (this.page === 0) return;
    }
}

*/